import { formatSetup } from '../../../helpers/format-setup';
import { module, test } from 'qunit';

module('Unit | Helper | format setup');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = formatSetup(42);
  assert.ok(result);
});
